name: starter
description: folders for initial project by MicroProgramers
version: "1.0.5"
vars:
  appname:
    type: string
    prompt: What is your full app name?

  projectName:
    type: string
    prompt: Write exact project name?

  serverKey:
    type: string
    prompt: Enter server key for firebase messaging?

  packageName:
    type: string
    prompt: Write exact package name of android?